name: Publish VS Code extension

# Publishes vscode/ to the VS Code Marketplace (and OpenVSX, if its token is set).
# Trigger it manually from the Actions tab, or by pushing a `vscode-v*` tag.
# Dormant until the secrets exist: each publish step no-ops (with a warning)
# when its token is absent, so the run stays green. Set the repo secrets
# VSCE_PAT (Marketplace publisher `auspeo`) and optionally OVSX_PAT to go live.

on:
  workflow_dispatch:
  push:
    tags: ['vscode-v*']

permissions:
  contents: read

jobs:
  publish:
    name: Package + publish
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413 # v6.1.0
        with:
          version: 10.26.2
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '22'
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Build extension
        run: pnpm --filter oauthlint-vscode build
      - name: Publish to VS Code Marketplace
        working-directory: vscode
        env:
          VSCE_PAT: ${{ secrets.VSCE_PAT }}
        run: |
          if [ -z "$VSCE_PAT" ]; then
            echo "::warning::VSCE_PAT not set — skipping Marketplace publish."
            exit 0
          fi
          set +e
          OUT=$(pnpm dlx @vscode/vsce@3.9.2 publish --no-dependencies -p "$VSCE_PAT" 2>&1); CODE=$?
          echo "$OUT"
          # Idempotent: re-running to publish a new marketplace must not fail
          # just because this version is already on the VS Code Marketplace.
          if [ $CODE -ne 0 ]; then
            echo "$OUT" | grep -qiE 'already exists|already published|same or greater version|is already' \
              && echo "::notice::Version already on the VS Code Marketplace — skipping." \
              || exit $CODE
          fi
      - name: Publish to OpenVSX
        working-directory: vscode
        env:
          OVSX_PAT: ${{ secrets.OVSX_PAT }}
        run: |
          if [ -z "$OVSX_PAT" ]; then
            echo "::warning::OVSX_PAT not set — skipping OpenVSX publish."
            exit 0
          fi
          set +e
          OUT=$(pnpm dlx ovsx@1.0.2 publish --no-dependencies -p "$OVSX_PAT" 2>&1); CODE=$?
          echo "$OUT"
          if [ $CODE -ne 0 ]; then
            echo "$OUT" | grep -qiE 'already exists|already published' \
              && echo "::notice::Version already on Open VSX — skipping." \
              || exit $CODE
          fi
