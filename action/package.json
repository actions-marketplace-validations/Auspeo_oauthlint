{
  "name": "oauthlint-action",
  "version": "0.2.0",
  "private": true,
  "description": "Docker-based GitHub Action wrapping oauthlint.",
  "type": "module",
  "scripts": {
    "build": "echo 'no build — Docker image is built by GitHub at runtime' && exit 0",
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "22.20.2",
    "typescript": "5.9.3",
    "vitest": "2.1.9",
    "yaml": "2.9.0"
  },
  "license": "MIT"
}
